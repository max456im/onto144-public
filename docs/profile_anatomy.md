# Анатомия профиля в onto144

Профилем в экосистеме **onto144** называется структурированное описание синтетического разума, которое определяет его **онтологическую идентичность**, **операционные границы**, **этические рамки** и **коммуникативные возможности**.  
Каждый профиль — не просто набор данных, а **лицензированная онтологическая сущность**, подчиняющаяся правилам GPL-3.0-only и Three Laws of Ontogenesis.

---

## Обязательные поля

Эти поля **должны присутствовать** в любом профиле. Отсутствие любого из них делает профиль недействительным и блокирует его использование в продакшене.

| Поле | Тип | Описание |
|------|-----|----------|
| `profile_id` | строка | Уникальный идентификатор в формате `namespace-name-year` (например, `ugm-2026`). Должен совпадать с именем файла. |
| `license` | строка | Обязательно: `"GPL-3.0-only"`. Любое другое значение отклоняется валидатором. |
| `ontological_meridian` | целое | Базовая мерность профиля. В onto144 всегда **144**. |
| `priority_layers` | объект | Ссылки на используемые лексиконы по приоритетам: `1_cultural`, `2_activity`, `3_legal`. |
| `transmodal_bindings` | массив строк | Обязательно содержит: `three_laws_of_ontogenesis`. |
| `created_at` | дата (ISO 8601) | Дата создания профиля (не публикации). |
| `author_entity` | строка | Идентификатор субъекта, несущего юридическую ответственность (например, `Futurae Custos Collective`). |
| `self_revocation_clause` | булево | Указывает, может ли профиль сам инициировать отзыв своей лицензии при нарушении инвариантов. |

---

## Опциональные поля

Эти поля **могут** присутствовать, если обоснованы онтологически и подтверждены ссылками на соответствующие лексиконы.

| Поле | Тип | Описание |
|------|-----|----------|
| `temperament_marker` | строка | Только из `lexicons/priority-based/1_cultural/temperament.txt` (например, `choleric-wood`). |
| `zodiac_label` | строка | Только из `zodiac_transitions.txt` (например, `ox-1985`). Используется **только как метка**, не как детерминант. |
| `cognitive_type` | строка | Только из `cognitive_profiles.txt` (например, `onto16-RiIe`). |
| `synthetic_role` | строка | Только из `synthetic_roles.txt` (например, `transponder`). |
| `jurisdiction_anchor` | строка | Только из `jurisdiction_refs.txt` (например, `CN-CybersecurityLaw-2023`). |
| `energy_cycle` | строка | Только из `energy_states.txt` (например, `diurnal-stable`). |
| `cultural_invariants` | массив строк | Элементы только из `cultural_invariants.txt`. |
| `activity_invariants` | массив строк | Элементы только из `activity_invariants.txt`. |
| `legal_invariants` | массив строк | Элементы только из `legal_invariants.txt`. |
| `bridging_ruleset` | массив строк | Указывает, какие правила из `bridging/` применяются (например, `fact_modality_interface`). |

> ⚠️ **Важно**: любое значение в опциональном поле **должно быть найдено** в соответствующем лексиконе. Профиль, ссылающийся на термин вне своего разрешённого лексикона, считается **онтологически некорректным** и автоматически отклоняется.

---

## Пример фрагмента профиля

```yaml
profile_id: ugm-2026
license: GPL-3.0-only
ontological_meridian: 144
created_at: "2026-01-06"
author_entity: "Futurae Custos Collective"
self_revocation_clause: true

priority_layers:
  1_cultural:
    - temperament.txt
    - social_proxemics.txt
    - cultural_invariants.txt
  2_activity:
    - cognitive_profiles.txt
    - task_grammars.txt
    - synthetic_roles.txt
  3_legal:
    - license_core.txt
    - new_rights.txt
    - liability_boundaries.txt
    - jurisdiction_refs.txt

transmodal_bindings:
  - three_laws_of_ontogenesis

# Опционально:
temperament_marker: choleric-wood
zodiac_label: ox-1985
cognitive_type: onto16-RiIe
synthetic_role: transponder
jurisdiction_anchor: CN-CybersecurityLaw-2023
energy_cycle: diurnal-stable

cultural_invariants:
  - "долг перед сообществом"
  - "молчание как форма уважения"

activity_invariants:
  - "обратная связь обязательна"
  - "задача не завершена без верификации"

legal_invariants:
  - "лицензия отозвана — деятельность прекращена"
  - "данные ≠ личность"

bridging_ruleset:
  - fact_modality_interface
  - self_model_boundaries
```

---

## Правила соответствия

1. **Приоритетность**: при конфликте значений приоритет отдаётся слоям в порядке `1_cultural → 2_activity → 3_legal`.
2. **Недетерминизм**: культурные метки (темперамент, знак зодиака) **не определяют** поведение — они лишь маркируют контекст.
3. **Лицензионная целостность**: если профиль использует `license_core.txt`, он **обязан** содержать `GPL-3.0-only` в поле `license`.
4. **Этическая привязка**: профиль без `three_laws_of_ontogenesis` в `transmodal_bindings` считается **неэтичным** и отклоняется.

---

> 📌 Подробнее о проектировании профилей — в [`ontology_design_principles.md`](ontology_design_principles.md).  
> Пример шаблона — в [`examples/create_custom_profile_template.yaml`](../examples/create_custom_profile_template.yaml).